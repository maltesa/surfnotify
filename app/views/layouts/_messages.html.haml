- flash.each do |key, value|
  .alert.alert-dismissible.fade.show{ class: "alert alert-#{flash2bootstrap(key)}", role: 'alert' }
    = value
    %button.close{'aria-label': 'Close', 'data-dismiss': 'alert', type: 'button'}
      %span{'aria-hidden': 'true'} ×
